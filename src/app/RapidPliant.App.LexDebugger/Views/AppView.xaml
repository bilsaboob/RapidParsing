<RapidView x:Class="RapidPliant.App.LexDebugger.Views.AppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <RapidGrid Orientation="Vertical">

        <RapidGrid Items="{Binding Path=LexPatterns}" set.Width="*" Orientation="Horizontal">
            <Button FontSize="20" FontFamily="Consolas" VerticalContentAlignment="Center" Click="{BindingAction Refresh}" Width="80">Rfresh</Button>
            <TextBox set.Width="*" FontSize="20" FontFamily="Consolas" VerticalContentAlignment="Center" Text="{Binding Path=Pattern}"></TextBox>
            <Button FontSize="20" FontFamily="Consolas" VerticalContentAlignment="Center" Click="{BindingAction Remove}">Remove</Button>
        </RapidGrid>

        <RapidGrid set.Width="*" Orientation="Horizontal">
            <Button FontSize="20" FontFamily="Consolas" VerticalContentAlignment="Center" Click="{BindingAction AddPattern}" Width="80">Add</Button>
            <TextBox set.Width="*" FontSize="20" FontFamily="Consolas" VerticalContentAlignment="Center" Text="{Binding Path=NewLexPattern}"></TextBox>
        </RapidGrid>
        
        <GraphView Graph="{Binding Path=Graph}" set.Height="*"></GraphView>
    </RapidGrid>
</RapidView>
